{
  "name": "邻星",
  "version": "1.0.0",
  "description": "邻星 Android App",
  "main": "index.js",
  "scripts": {
    "build": "npx cap sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
